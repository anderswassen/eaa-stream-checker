{
  "name": "eaa-stream-checker",
  "version": "0.0.13",
  "description": "EAA Stream Checker — accessibility compliance tool for streaming services",
  "type": "module",
  "main": "dist/src/server.js",
  "scripts": {
    "postinstall": "apk add --no-cache chromium nss freetype harfbuzz ca-certificates ttf-freefont 2>/dev/null || true",
    "build": "tsc && cd frontend && npm ci && npm run build && cp -r dist ../public",
    "dev": "tsx watch src/server.ts",
    "start": "node dist/src/server.js",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test tests/unit/*.test.ts",
    "test:integration": "tsc && node --test dist/tests/integration/*.test.js"
  },
  "dependencies": {
    "@axe-core/playwright": "^4.10.1",
    "@fastify/cors": "^10.0.2",
    "@fastify/static": "^9.0.0",
    "fastify": "^5.2.1",
    "playwright-core": "^1.58.2",
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.4",
    "@types/uuid": "^10.0.0",
    "pino-pretty": "^13.1.3",
    "tsx": "^4.19.3",
    "typescript": "^5.7.3"
  }
}
